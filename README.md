# HarmonyOS 游客服务系统

本仓库是《游客服务系统》课程项目单仓代码库（Monorepo）。

## 仓库结构

- `backend/`：Spring Boot + MySQL + JWT + Flyway
- `frontend/harmony-app/`：HarmonyOS ArkTS 前端（Stage Model）
- `docs/`：接口契约与联调测试文档

## 快速开始

1. 克隆仓库并切换到 `main`。
2. 启动后端依赖（MySQL Docker）。
3. 启动后端服务。
4. 使用 DevEco 打开 `frontend/harmony-app` 运行前端。

## 后端启动

```bash
cd backend
docker compose up -d
mvn spring-boot:run
```

Swagger：
- http://localhost:8081/swagger-ui.html

## 前端说明

- 在 DevEco Studio 打开 `frontend/harmony-app`。
- 默认后端地址：`http://10.0.2.2:8081`（模拟器访问宿主机）。
- 真机联调可本地改为局域网地址，但禁止提交个人 IP。

## 演示账号

- 游客：`visitor / visitor123`
- 平台管理员：`admin / admin123`
- 投诉处理员：`handler / handler123`
- 酒店管理员：`hoteladmin / hoteladmin123`
- 系统管理员：`sysadmin / sysadmin123`
- 兼容账号：`writer / writer123`、`approver / approver123` 仍可登录，但已并入平台管理员角色。

## 分支协作规范

- 主分支：`main`（建议启用保护规则）
- 开发分支命名：
  - 功能：`feature/<module>-<task>`
  - 修复：`fix/<module>-<issue>`
- 建议流程：分支开发 -> PR 审查 -> 合并 `main`

## 版本说明

### 2026-04-17（v0.2.0 阶段二前后端统一版）

- 将项目从“骨架阶段”推进到“可联调演示阶段”，完成当时前后端角色与接口的第一轮统一。
- 前端：
  - 完成多角色导航与入口分离的早期版本。
  - 落地投诉主链路页面能力：提交、列表、详情、审批、处理、结案、评价。
  - 落地应急主链路页面能力：建稿、编辑、提交审批、审批发布、游客查询。
  - 查询模块切换到酒店、景区线路、餐饮娱乐演出、天气路况新接口。
  - 加强仓库卫生：忽略 `entry/.preview/` 与 `oh_modules/`。
- 后端：
  - 扩展多角色演示账号初始化。
  - 开放查询路由：`/api/query/hotels`、`/api/query/scenic-spots`、`/api/query/routes`、`/api/query/dining`、`/api/query/entertainment`、`/api/query/performances`、`/api/query/weather`、`/api/query/traffic`。
  - 保留旧查询路由兼容，避免旧前端联调中断。
- 已知事项：
  - 当时未执行完整本地 build/模拟器验证，需由测试组回归。
  - 要持续避免提交 `.preview`、`oh_modules`、个人 IP、个人密码。

### 2026-04-18（v0.2.1 需求差距交付包）

- 目标：先交付“需求文档 vs 现有代码”的可审阅差距结果，明确已实现、部分实现、未实现与冲突项。
- 新增文档：
  - `docs/requirements-gap-delivery.md`
  - `docs/requirements-traceability-matrix.md`
  - `docs/gap-backlog.md`
  - `docs/api-compatibility-diff.md`
  - `docs/db-migration-incremental-plan.md`
  - `docs/release-collaboration-gates.md`
- 兼容性承诺：保持现有架构边界，组员拉取后无需额外本地配置即可启动。
- 接口策略：旧接口保持可用，新增能力按增量方式落地。
- 发布要求：后续每次提交按模板补充 README 版本说明与验证结果。

### 2026-04-18（P0 闭环统一落地）

- 投诉链路补齐：新增驳回、分派、时间线，状态扩展 `REJECTED`。
- 权限收口：
  - `approve/reject/assign`：`ADMIN`
  - `process`：`COMPLAINT_HANDLER | ADMIN`
  - `close`：`ADMIN`
  - `VISITOR`：创建、查看本人、结案后评分
- 应急游客列表加入有效期过滤（仅返回有效窗口内 `PUBLISHED`）。
- Flyway 增量迁移落地：`V2`、`V3`。
- 异常映射增强：`NoSuchElementException -> 404/NOT_FOUND`。

### 2026-04-18（P1 核心包：认证 + 筛选 + 查询持久化）

- 认证扩展：
  - 新增 `POST /api/auth/register`
  - 新增 `POST /api/auth/forgot-password/request-code`
  - 新增 `POST /api/auth/forgot-password/reset`
  - 验证码规则：6 位数字、10 分钟有效、单次使用、仅最新有效
- 投诉筛选：`GET /api/complaints` 支持 `status/createdBy/assignee/keyword/from/to`。
- 查询持久化：`/api/query/*` 改为 MySQL 表读取，保留兼容接口。
- Flyway 新增：
  - `V4__query_domain_tables.sql`
  - `V5__auth_extension_tables.sql`
- 前端落地：登录页支持登录/注册/忘记密码三态；投诉页新增筛选区与重置动作。
- 角色规则加强：应急管理接口加入方法级权限控制，避免普通角色误调用。

#### P1 回滚说明

如需回滚 P1，请按以下顺序执行：
1. 代码回滚到 P0 对应提交（通过 Git tag 或提交号）。
2. 数据库仅做向后兼容读取，不执行破坏性删除（`V4/V5` 表可保留）。
3. 前端恢复到不依赖注册/忘记密码与投诉筛选参数的版本。

> 说明：课程环境建议“代码回滚、数据保留”，避免影响已有联调数据。

### 2026-04-18（P2 游客端 UI 大改：四栏导航 + 视觉重构）

- 游客端重构为底部四栏：`首页 / 服务 / 投诉 / 应急`，替换原顶部多标签导航。
- 新增游客首页 Dashboard：天气摘要、服务金刚区、今日推荐景点卡、投诉进度摘要卡。
- 服务页整合酒店/景区/线路，并支持房态标签与景区客流热度展示。
- 游客投诉页重排为“提交表单 + 附件占位 + 时间轴进度 + 历史记录”结构。
- 游客应急页重排为“预警卡 + 路况示意地图卡 + 实时提醒列表”结构（静态示意地图方案）。
- 前端新增可复用 UI 组件库：悬浮卡片、主按钮、状态徽标、分组标题、空态卡、时间轴节点、筛选芯片。
- 新增数据库增量脚本：`V6__ui_enhancement_fields.sql`，扩展可选字段：
  - 酒店：`availability_status`、`cover_image_url`
  - 景区：`crowd_heat`、`cover_image_url`
  - 应急：`alert_level`、`alert_type`
  - 路况：`severity_level`
- 接口兼容策略：旧路径与旧字段保持可用，新字段按“可选”返回，旧前端不受影响。

### 2026-04-20（P3 UI Baseline 对齐：服务二级切换 + 餐饮娱乐演出增强）

- 游客端保留底部四栏：`首页 / 服务 / 投诉 / 应急`，并在“服务”页新增二级切换：`酒店景区线路 / 餐饮娱乐演出`。
- 首页升级为基线风格：天气+欢迎语、蓝色服务金刚区（新增“餐饮娱乐”入口）、动态推荐卡（景点/演出预告）。
- 投诉页采用圆角输入与闭环时间轴：`已提交 / 审批中 / 处理中 / 已结案`，本轮不提供媒体上传入口。
- 应急页强化预警分级与路况卡，增加封闭路段显著标识与提醒列表展示一致性。
- 餐饮娱乐演出页新增分类导航、区域/价格筛选、营业状态高亮、距离提示、导航按钮与演出票务状态展示。
- 查询能力扩展（保持兼容）：酒店/景区支持等级、区域、价格筛选；餐饮/娱乐/演出新增可选字段（Logo、距离、营业状态、票务状态、导航坐标）与排序/限量参数。
- 新增数据库迁移：`V7__dining_performance_extensions.sql`，用于餐饮/娱乐/演出字段扩展与示例数据补齐。
- 同版本缺陷修复：修复 `admin/approver/sysadmin` 角色标签页切换不稳定问题（页签状态重置 + 交互容器优化）。
- 同版本缺陷修复：演示账号改为启动时校准（含 `hoteladmin / hoteladmin123`），避免历史数据导致默认口令无法登录。
- 本版本明确不包含：原子服务卡片、负一屏能力、跨端设备流转、媒体上传能力。

### 2026-04-20（P4 五角色 RBAC 收敛）

- 角色模型收敛为五类：`VISITOR`、`ADMIN`、`COMPLAINT_HANDLER`、`HOTEL_ADMIN`、`SYSTEM_ADMIN`。
- 旧 `writer/approver` 演示账号保留，但统一并入 `ADMIN` 平台管理员；旧 `HOTEL_MANAGER/HOTELADMIN` 兼容映射为 `HOTEL_ADMIN`。
- 后端登录与 `/api/auth/me` 增加 `permissions` 返回，由角色派生权限，不新增权限表。
- 权限边界调整：
  - `ADMIN`：投诉审批/驳回/分派/结案、应急管理、导流营销、旅游资源库入口。
  - `COMPLAINT_HANDLER`：仅处理分派给自己的投诉。
  - `HOTEL_ADMIN`：酒店房态/酒店信息维护，不进入平台运营后台。
  - `SYSTEM_ADMIN`：系统管理入口，当前保留用户/角色/权限/日志占位。
- 酒店管理接口 `/api/hotels/admin` 收紧为仅 `HOTEL_ADMIN` 可调用，平台管理员不再拥有酒店资料新增/编辑/删除权限。
- 前端入口同步为五角色：首页不再展示独立 `writer/approver` 分支，`admin` 进入平台工作台，`sysadmin` 进入系统管理中心。
- Flyway 新增：`V9__normalize_five_roles.sql`，用于历史角色值和演示账号角色归一。

### 2026-04-21（分支整理与远程发布）

- 完成当前 `meizhouyu666-p1-core-package` 分支代码整理并推送远程，保留五角色 RBAC 收敛方案与兼容迁移。
- 权限主线保持“角色 + 权限点”双层控制：后端统一鉴权，前端按角色动态收敛标签页与功能入口。
- 演示账号与历史角色值保持兼容：`writer/approver` 兼容登录并归并为 `ADMIN`，酒店角色统一为 `HOTEL_ADMIN`。
- 文档同步更新：接口地址、演示账号、角色边界、测试要点与回归说明均与当前分支实现对齐。

### 2026-04-21（v0.3.0 第二阶段完整性收口版）

- 本版本完成“前端多角色交互落地 + 后端全链路真实补齐”的集中收口，是当前课程项目第一版较完整的可演示版本。

#### 后端链路补齐

- 投诉链路继续沿用既有主路径，补齐并稳定以下闭环：
  - 游客提交 -> 管理员审批/驳回 -> 管理员分派 -> 处理员处理 -> 管理员结案 -> 游客评价。
- 投诉动作统一写入审计日志：提交、审批、驳回、分派、处理、结案、评价全程留痕。
- 应急链路补齐真实驳回接口：
  - 新增 `POST /api/emergency/admin/{id}/reject`
  - 应急状态支持 `DRAFT / PENDING_APPROVAL / PUBLISHED / REJECTED`
  - 驳回后允许继续编辑并重新提交审批。
- 旅游资源库后端落地：
  - 新增 `/api/resources/{category}` 管理接口。
  - 直接管理现有 `query_*` 资源表，不额外引入独立 CMS 表。
  - 支持景区景点、游览线路、餐饮、演出四类资源的查询、新增、编辑、上架、下架、删除。
- 酒店管理员一账号一酒店隔离落地：
  - `users` 增加 `managed_hotel_id` 与 `enabled`
  - `HOTEL_ADMIN` 仅能看到和维护自己绑定酒店。
  - 平台管理员继续不拥有酒店资料写权限。
- 系统管理员真实后端能力落地：
  - 新增系统管理接口：用户管理、审计日志查询、系统参数读写。
  - 新增审计日志表与系统参数表。
  - 系统管理员可创建账号、修改角色、绑定酒店、重置密码、启停账号、查询日志、修改参数。
- 认证联动增强：
  - 登录、注册、验证码申请、密码重置都接入审计日志。
  - 登录与 `/api/auth/me` 继续返回 `role + permissions`。
  - 已停用账号禁止登录。
- 数据库迁移：
  - 新增 `V10__system_resource_backend_extensions.sql`
  - 统一补齐资源管理、系统参数、审计日志与账号绑定字段。

#### 前端交互与页面收口

- 平台管理员工作台重构完成：
  - 统一为四卡片业务入口：投诉管理、应急信息、导流营销、旅游资源库。
  - 移除平台管理员上的系统管理入口，避免与系统管理员权限重叠。
  - 调整平台端顶部头卡、标题字号、主按钮尺寸，形成统一视觉层级。
- 投诉管理页重构为标准审批与结案链路视图：
  - 待审批派单、处理确认中、待审核结案、已归档四阶段分组。
  - 删除重复头卡，压缩标签字号，适配手机宽度。
- 应急管理页重构为发布与管控中心：
  - 展示中、待审批、草稿/驳回三态切换。
  - 支持搜索、起草、编辑、提交审批、审批发布、删除。
  - 预警级别与类型的选择器改为更明显的可选卡片。
- 旅游资源库页重构完成：
  - 景区景点、游览线路、餐饮娱乐、演出团体四类资源统一在一个 CMS 页面维护。
  - 支持搜索、上下架、编辑、删除与新增资源。
  - 顶部分类卡与底部新增按钮尺寸统一到平台端设计规范。
- 系统管理中心完善：
  - 完成系统运行状态、账号与权限、操作日志审计、RBAC 角色配置、系统参数设置入口。
  - 修复系统管理页中文乱码并统一四张功能卡片图标与点击动效。
- 酒店管理员端完成商户专属工作台：
  - 聚焦本酒店房态录入与一键同步。
  - 修复房型步进器仅更新顶部总数、不刷新单卡数量的问题。
- 游客端服务页完成双模块深化：
  - 酒店景区线路与餐饮娱乐演出分离为两个二级入口。
  - 酒店住宿模块重构为：
    - 全部 / 星级 / 民宿切换
    - 官方导流特惠推荐横向卡片
    - 房态标签与预订按钮联动
    - 满房自动置灰显示“已售罄”
  - 餐饮娱乐演出模块重构为：
    - 推荐 / 餐饮 / 娱乐 / 演出四类切换
    - 金刚区快捷入口
    - 营业状态、票务状态、动作按钮差异化展示。
- 游客端应急页与服务页多处布局问题修复：
  - 修复列表错误居中、筛选区跟随滚动、图片空白占位、中文乱码、标签样式不统一等问题。

#### 文档与联调

- 更新 `docs/openapi-minimal.yaml`，同步应急驳回、资源管理、系统管理等新增接口。
- 更新 `docs/testing-runbook.md`，补充五角色测试路径与后端全链路验证点。
- README 版本说明与主分支历史版本合并，保证版本时间线连续完整。

#### 验证与注意事项

- 后端已完成 `mvn -q -DskipTests compile` 级别编译验证。
- 前端仍以 DevEco 本地编译与测试组回归为最终验收依据。
- 仓库继续保持卫生约束：
  - 禁止提交 `.preview/**`
  - 禁止提交 `oh_modules/**`
  - 禁止提交个人 IP 与个人数据库密码

### 2026-04-22（v0.3.1 平台联调深化版）

- 本版本继续在 `meizhouyu666-p1-core-package` 分支上集中推进，重点从“功能可演示”深化到“平台端、商户端、游客端真实数据联动”，并为后续合并 `main` 做版本说明归档。

#### 后端能力深化

- 旅游资源库从“模板化字段”改为“分类真实字段管理”：
  - 景区景点：支持景区区域、开放时间、票价、等级、热度等字段。
  - 游览线路：支持建议时长、难度、适合人群、线路节点等字段。
  - 餐饮：支持营业时间、人均价格、推荐菜、距离、导航等字段。
  - 演出：支持票价、余票、票务状态、演出团队、场馆、演出时间等字段。
- `/api/resources/{category}` 系列接口不再使用写死默认值，更新时仅覆盖显式传入字段，未传字段保留原值，避免“改标题导致价格/等级被模板重置”。
- 平台导流营销后端正式落地：
  - 新增营销配置字段迁移：`V11__hotel_marketing_config.sql`
  - 新增 `/api/marketing/hotels` 读接口与酒店营销配置更新能力
  - 平台端可真实开启/关闭推荐、维护营销标签、优先级与推荐说明
  - 导流页的客流、热度、建议改为后端聚合推导值，不再是前端静态摆设
- 酒店房型库存后端正式落地：
  - 新增迁移：`V12__hotel_room_inventory.sql`
  - 新增酒店房型库存表与房型读写接口
  - `HOTEL_ADMIN` 可真实读取与更新自己绑定酒店的房型库存
  - 酒店整体房态由房型库存自动推导，不再依赖前端本地演示态
- 系统管理后端继续稳定：
  - 用户管理、审计日志、系统参数读写与固定五角色权限展示继续沿用 v0.3.0 方案
  - 角色派生权限继续作为主线，不引入动态权限表
- 角色与分权进一步收口：
  - 平台端继续围绕 `ADMIN / APPROVER / COMPLAINT_HANDLER / HOTEL_ADMIN / SYSTEM_ADMIN / VISITOR` 进行差异化入口与能力控制
  - 应急模块按“发布/修改/删除”和“审批”分别收口，避免平台端所有账号拥有完全相同的应急权限

#### 前端联调深化

- 平台管理工作台从演示态升级为真实数据摘要页：
  - 蓝色看板显示待办投诉、生效预警、客流/审批负载等真实聚合值
  - “最新待办”改为后端投诉/应急数据驱动
  - “查看全部”改为按账号权限跳转真实业务模块
- 平台导流营销页改为真实联调版：
  - 读取后端 `/api/marketing/hotels`
  - 展示真实推荐酒店、剩余房量、营销标签与导流建议
  - 开启/关闭推荐不再是本地演示开关，而是直接更新后端配置
- 平台旅游资源库改为真实 CMS 页面：
  - 分类切换、搜索、上下架、编辑、删除、新增都接入真实资源库接口
  - 前端表单与后端分类字段对齐，不再停留在本地假数据
- 系统管理中心前端接入真实系统接口：
  - 账号与权限、日志审计、系统参数不再是纯展示卡片
  - 可直接创建账号、绑定酒店、启停账号、重置密码、查询日志、修改参数
- 游客端酒店服务深化：
  - 酒店推荐区开始消费平台端真实营销配置
  - 推荐酒店按后端优先级与推荐标签展示
  - 酒店卡片开始显示真实剩余房量与总房量
  - 满房/紧张/充足继续与预订按钮联动
- 游客端餐饮娱乐演出页改为真实查询联调版：
  - 推荐、餐饮、娱乐、演出四类切换改为真实 `/api/query/*` 数据驱动
  - 状态标签和操作按钮按餐饮/娱乐/演出类型差异化展示
- 酒店管理员端继续深化：
  - 房型数量可真实同步后端并影响平台导流与游客端展示
  - 商户端保持“一账号一酒店”，不暴露平台其他业务模块
- 投诉游客端进度面板修复：
  - 无历史投诉时改为空态提示，不再错误显示“已提交”
  - 有历史投诉时继续展示后端最新投诉的真实状态

#### 文档与协作归档

- `docs/openapi-minimal.yaml` 已继续同步资源库、营销、酒店房型库存等真实接口说明与示例
- 现有 README 版本时间线保留主分支历史说明，并以当前分支为准继续追加 v0.3.1 版本记录
- 本版本作为“后续合并 `main` 的归档说明基础版本”，主干冲突处理以当前分支实现为主，但保留历史 README 版本说明不丢失

#### 当前阶段说明

- 本版本已经从“二阶段完整性收口”推进到“平台端/商户端/游客端真实联调深化”阶段
- 当前剩余问题主要集中在少量前端交互细节与布局微调，不再是后端链路缺失或模块级空白
- 建议后续合并 `main` 时优先保留当前分支代码，实现与 README 版本说明同时进入主干
